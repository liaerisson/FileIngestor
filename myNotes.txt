concat --> strings are immutable so concat can't be called on the string, but rather the new value is assigned to the string
  * String a = "value";
  * a = a.concat(" new value");
  results in:
  * System.out.println(a) = value new value
StringBuilder --> great substitute for concat, it functions smoother and has an easier way to add new lines (content.append(System.lineSeparator()))

https://www.youtube.com/watch?v=kXcc6AI_8-Q --> tokenization guide
https://www.youtube.com/watch?v=MH384SzszLE --> StringTokenizer
toLowerCase, split (and delimiters), replaceAll

//realizing that concat doesn't change strings (which are immutable)
        String s = "Strings are immutable ";
        s.concat("hello");
        System.out.println(s);

        String t = "Strings are immutable ";
        t = t.concat("hello");
        System.out.println(t);

The tokenizer is done, but I think I should go back and add more cases later
  * when there is a dash connecting a phrase --> the phrase and 2 words? how about split words?
  * remove some basic words (the, and, etc...)
  * if something ends with a space, does that cause

Inverted Index
  * need to track both the terms and the frequency of those terms in each document

I need to decide which methods should be static since there isn't really a need for instances of DocumentLoaders or InvertedIndexes.
  * made tokenize static

In QueryProcessor, I need to sort the frequencies of the selected word in each document.
  * as such, I need a sorting method with a comparator
  * this involves Comparator.comparingInt() (Integer being the type compared)

SearchResult --> a type of instance with the document and the number of occurrences of the searched words
SearchEngine --> how all the methods and mechanisms of searching are integrated to an easier interface

REST API
  * this bridges a HTTP ability with the program itself, so it can be run from a browser
  * SpringBoot needs an application class along with a controller class
  * there are labels for the type of purpose of a class (@SpringBootApplication, @RestController, @Component, @Service)
DTO --> Data Transfer Object
  *